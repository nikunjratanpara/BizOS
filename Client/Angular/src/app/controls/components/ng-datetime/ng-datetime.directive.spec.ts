import { NgDatetimeDirective } from './ng-datetime.directive';

describe('NgDatetimeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDatetimeDirective();
    expect(directive).toBeTruthy();
  });
});
