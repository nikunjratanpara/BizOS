import { NgTypeaheadDirective } from './ng-typeahead.directive';

describe('NgTypeaheadDirective', () => {
  it('should create an instance', () => {
    const directive = new NgTypeaheadDirective();
    expect(directive).toBeTruthy();
  });
});
